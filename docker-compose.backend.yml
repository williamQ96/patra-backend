# Supported local/dev stack for the active Patra backend.
# This file replaces the suspended Neo4j-oriented docker-compose.yml for new work.

services:
  postgres:
    image: postgres:16
    container_name: patra-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-patra}
      POSTGRES_USER: ${POSTGRES_USER:-patra}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-patra-dev-password}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - patra_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-patra} -d ${POSTGRES_DB:-patra}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - default
      - ckn-network

  patra-backend:
    build:
      context: .
      dockerfile: rest_server/Dockerfile
    container_name: patra-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PORT: 8000
      DATABASE_URL: postgresql://${POSTGRES_USER:-patra}:${POSTGRES_PASSWORD:-patra-dev-password}@postgres:5432/${POSTGRES_DB:-patra}
      PATRA_ASSET_INGEST_KEYS_JSON: '{"org-a":"change-me"}'
      TAPIS_AUTH_VALIDATION_ENABLED: ${TAPIS_AUTH_VALIDATION_ENABLED:-true}
      TAPIS_JWKS_URL: ${TAPIS_JWKS_URL:-}
      TAPIS_ISSUER: ${TAPIS_ISSUER:-}
      TAPIS_AUDIENCE: ${TAPIS_AUDIENCE:-}
      TAPIS_USERNAME_CLAIM: ${TAPIS_USERNAME_CLAIM:-tapis/username}
      TAPIS_TOKEN_LEEWAY_SECONDS: ${TAPIS_TOKEN_LEEWAY_SECONDS:-60}
      ALLOW_UNVERIFIED_TAPIS_TOKEN_DEV_ONLY: ${ALLOW_UNVERIFIED_TAPIS_TOKEN_DEV_ONLY:-false}
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    volumes:
      - ../../src:/app/src:ro
      - ../../PATRA:/app/PATRA
      - ./rest_server:/app/rest_server:ro
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/healthz', timeout=3)"]
      interval: 30s
      timeout: 5s
      start_period: 15s
      retries: 3

  patra-mcp:
    build:
      context: .
      dockerfile: mcp_server/Dockerfile
    container_name: patra-mcp
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-patra}:${POSTGRES_PASSWORD:-patra-dev-password}@postgres:5432/${POSTGRES_DB:-patra}
    ports:
      - "${MCP_PORT:-8050}:8050"

  cloudbeaver:
    image: dbeaver/cloudbeaver:latest
    container_name: patra-cloudbeaver
    ports:
      - "8978:8978"
    volumes:
      - cloudbeaver_data:/opt/cloudbeaver/workspace
    depends_on:
      postgres:
        condition: service_healthy

networks:
  ckn-network:
    external: true
    name: ckn-network

volumes:
  patra_postgres_data:
  cloudbeaver_data:
